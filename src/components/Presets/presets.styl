.preset
	&:hover
		cursor pointer
	&--active
		&:hover
			cursor default	
	&__title
		text-transform capitalize
	&__colors
		display -webkit-flex
		display -moz-flex
		display -ms-flex
		display -o-flex
		display flex
	&__color 
		width 100%
		height 3rem